import styled from 'styled-components';
import styles from '../../constants/stylesProperty';

export const FullscreenLoadingBlock = styled.div`
	height: 100vh;
	width: 100vw;
	background-color: ${styles.colors.white};
`;
